<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <!-- ViewModel or data binding 추가 가능 -->
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_gradient"
        tools:context=".ui.MainActivity"
        tools:deviceIds="tv">

        <!-- 타이틀 -->
        <com.example.customerlauncher.custom.CustomTitleView
            android:id="@+id/title_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:focusable="true"
            android:focusableInTouchMode="true"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginStart="24dp"
            android:layout_marginTop="24dp" />

        <!-- 날씨 카드 그룹 (수평 정렬) -->
        <LinearLayout
            android:id="@+id/weather_card_group"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:orientation="horizontal"
            android:layout_marginTop="16dp"
            android:layout_marginStart="24dp"
            app:layout_constraintTop_toBottomOf="@id/title_view"
            app:layout_constraintStart_toStartOf="parent">

            <!-- 메인 날씨 카드 -->
            <include
                layout="@layout/weather_card"
                android:id="@+id/weather_card"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <!-- 서브 카드 1 -->
            <include
                layout="@layout/weather_card"
                android:id="@+id/weather_card_tomorrow"
                android:layout_width="wrap_content"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp" />

            <!-- 서브 카드 2 -->
            <include
                layout="@layout/weather_card"
                android:id="@+id/weather_card_day_after"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:layout_marginStart="16dp" />
        </LinearLayout>

        <!-- 콘텐츠 영역 -->
        <FrameLayout
            android:id="@+id/main_browse_fragment"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/weather_card_group"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="32dp"
            tools:ignore="MergeRootFrame" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
